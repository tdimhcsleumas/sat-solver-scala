#!/usr/bin/env bash

set -e

[[ -z "$SAT_SOLVER_CLI_ROOT" ]] && echo "SAT_SOLVER_CLI_ROOT must be defined" && exit 1

jar_path=$SAT_SOLVER_CLI_ROOT/sudoku-cli/target/scala-2.12/sudoku-cli.jar

java -jar $jar_path $@
